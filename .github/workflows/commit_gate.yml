on: [push]

jobs:
  run_tests_job:
    runs-on: ubuntu-latest
    name: Run qdaim tests
    steps:
      # To use this repository's private action,
      # you must check out the repository
      - name: Checkout
        uses: actions/checkout@v2
      - name: Run tests action step
        uses: ./.github/actions
        id: run_tests
